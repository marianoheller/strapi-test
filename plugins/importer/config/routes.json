{
  "routes": [
    {
      "method": "POST",
      "path": "/pizza",
      "handler": "Importer.upload",
      "config": {
        "policies": [],
        "description": "Upload a file",
        "tag": {
          "plugin": "importer",
          "name": "File"
        }
      }
    },
    {
      "method": "GET",
      "path": "/pizza/count",
      "handler": "Importer.count",
      "config": {
        "policies": [],
        "description": "Retrieve the total number of uploaded pizzas",
        "tag": {
          "plugin": "importer",
          "name": "File"
        }
      }
    },
    {
      "method": "GET",
      "path": "/pizza",
      "handler": "Importer.find",
      "config": {
        "policies": [],
        "description": "Retrieve all pizza documents",
        "tag": {
          "plugin": "importer",
          "name": "File"
        }
      }
    }
  ]
}
